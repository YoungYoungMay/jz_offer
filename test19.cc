//二叉树的镜像
#include <iostream>
using namespace std;

struct BinaryTreeNode
{
    int val;
    BinaryTreeNode* left;
    BinaryTreeNode* right;
};

void TreeInit(BinaryTreeNode** root)
{
    if(root == NULL)
        return;
    *root = NULL;
    return;
}

int main()
{

    return 0;
}
